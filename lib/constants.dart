import 'package:flutter/material.dart';

/// App-wide constants for GymLife v2.

/// Membership plan levels (ordered from lowest to highest).
const List<String> membershipLevels = ['Silver', 'Gold', 'Platinum'];

/// News categories available in the app.
const List<String> newsCategories = [
  'Promo',
  'Event',
  'Class Schedule',
  'General',
];

/// Brand blue used on buttons and accents.
const Color primaryColor = Color(0xFF4C7FFF);

/// Dark navy background used across the app.
const Color bgColor = Color(0xFF0F0F1E);

/// Dark app bar / panel color.
const Color appBarColor = Color(0xFF1A1A2E);

/// Secondary dark card color.
const Color cardColor = Color(0xFF16213E);

/// Signature accent (red / crimson).
const Color accentRed = Color(0xFFE94560);

/// Secondary accent (teal / mint).
const Color accentTeal = Color(0xFF4ECCA3);

/// Dark form card background used on the auth screens.
const Color formCardColor = Color(0xFF38393D);

/// Deep blue used on the auth hero area.
const Color darkBlue = Color(0xFF141A33);
