/// Global namespace used to scope app data inside the Realtime Database.
const String appIdentifier = 'gymlife-app';

/// Membership plan levels (ordered from lowest to highest).
const List<String> membershipLevels = ['Silver', 'Gold', 'Platinum'];

/// News categories available in the app.
const List<String> newsCategories = ['Promo', 'Acara', 'Jadwal Kelas', 'Info Umum'];
